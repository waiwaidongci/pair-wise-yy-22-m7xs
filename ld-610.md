# ld-610 文物修复档案协作平台

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述

**文物修复档案协作平台（relic-restore）**：面向博物馆修复团队的文物病害记录、修复方案、影像版本和审批归档平台。项目类型：**全栈 Web 应用**。

### 2. 核心实体（≥ 3 个，必须贯穿全栈）

1. **文物藏品（RelicItem）**
   - 字段：id, relic_code, name, era, material, collection_level, storage_location, current_condition
   - 关键关系：拥有病害、修复方案和影像版本
   - 业务动作：藏品建档、级别维护、状态更新
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

2. **病害记录（DamageRecord）**
   - 字段：id, relic_id, damage_type, position_desc, severity, discovered_by, discovered_at, image_url, status
   - 关键关系：触发 RestorationPlan
   - 业务动作：病害登记、分级、关闭
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

3. **修复方案（RestorationPlan）**
   - 字段：id, relic_id, damage_record_id, plan_title, method, risk_assessment, approval_status, owner_id
   - 关键关系：经审批后生成 RestorationStep
   - 业务动作：方案编制、审批、变更
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

4. **修复步骤（RestorationStep）**
   - 字段：id, plan_id, step_order, technique, material_used, operator_id, step_status, finished_at
   - 关键关系：记录实际修复过程
   - 业务动作：步骤执行、材料记录、质检
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

5. **影像版本（ImageVersion）**
   - 字段：id, relic_id, plan_id, version_no, image_type, file_path, capture_at, note
   - 关键关系：关联修复前后对比和步骤证据
   - 业务动作：上传影像、版本对比、归档
   - **此实体必须贯穿全栈，不得只在单一文件内实现。**

### 3. 核心页面（≥ 4 个，至少 2 个页面共用同一业务组件）

1. **修复工作台** `/dashboard`
   - 消费实体/模型：RelicItem + DamageRecord + RestorationPlan
   - 关键交互：待审批方案、重度病害、修复进度、影像归档量
   - 共享组件：StatCard, StatusBadge, SeverityBadge

2. **文物档案** `/relics`
   - 消费实体/模型：RelicItem + DamageRecord + ImageVersion
   - 关键交互：藏品列表、详情、病害和影像版本
   - 共享组件：StatusBadge, RelicInfoCard, ImageCompare

3. **病害记录** `/damages`
   - 消费实体/模型：DamageRecord + RelicItem + RestorationPlan
   - 关键交互：记录病害、标注位置、转修复方案
   - 共享组件：SeverityBadge, StatusBadge, TimelineList

4. **修复方案** `/plans`
   - 消费实体/模型：RestorationPlan + RestorationStep + DamageRecord
   - 关键交互：方案编辑、审批、步骤拆解
   - 共享组件：StatusBadge, ApprovalTimeline, StepList

5. **影像版本** `/images`
   - 消费实体/模型：ImageVersion + RelicItem + RestorationPlan
   - 关键交互：修复前后对比、版本说明、筛选归档
   - 共享组件：ImageCompare, StatusBadge, EmptyState

### 4. 横切关注点（≥ 2 个）

1. **JWT + RBAC（修复师/专家审批/档案员/访客）**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。
2. **操作日志（方案审批、影像归档、病害关闭）**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。
3. **全局错误处理**
   - 必须触达：数据库表、后端中间件、route/controller/service、前端路由守卫、store、组件按钮显隐、类型定义、日志模板、错误码。

### 5. 共享枚举/常量（≥ 2 组）

1. **RelicCondition**：STABLE / FRAGILE / DAMAGED / IN_RESTORATION / SEALED
   - 前端位置：`frontend/src/constants/RelicCondition.ts` 或同名模块
   - 后端位置：`backend/src/constants/RelicCondition.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
2. **PlanApprovalStatus**：DRAFT / SUBMITTED / APPROVED / REJECTED / ARCHIVED
   - 前端位置：`frontend/src/constants/PlanApprovalStatus.ts` 或同名模块
   - 后端位置：`backend/src/constants/PlanApprovalStatus.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置
3. **DamageSeverity**：LOW / MEDIUM / HIGH / CRITICAL
   - 前端位置：`frontend/src/constants/DamageSeverity.ts` 或同名模块
   - 后端位置：`backend/src/constants/DamageSeverity.*` 或同名模块
   - README 必须列出该枚举在前后端类型、构造器、日志模板、错误消息、筛选器和展示组件中的所有出现位置

### 6. 共享前端组件清单（≥ 3 个）

- `StatusBadge`
- `SeverityBadge`
- `ImageCompare`
- `ApprovalTimeline`
- `RelicInfoCard`

### 7. 共享 hooks/utils 清单（≥ 2 个）

- `usePlanApproval`
- `useImageVersionCompare`
- `usePagination`

### 8. 后端中间件清单（≥ 2 个）

- `authMiddleware`
- `rbacMiddleware`
- `auditLogMiddleware`
- `errorHandlerMiddleware`
- `requestLoggerMiddleware`

### 9. 技术要求

- 英文短名（用于 Docker Compose）：`relic-restore`
- 前端端口：`20110`
- 后端端口：`21110`
- 后端容器内部端口：`3000`
- 前端技术栈：React 18 + TypeScript + Vite + Ant Design + Zustand
- 后端技术栈：NestJS + TypeScript + Prisma
- 数据库：PostgreSQL 15
- 认证方式：JWT + RBAC
- 数据来源：全部本地数据库和种子数据，禁止接入第三方 API

### 复杂度下限与耦合目标

本项目用于验证跨文件协同改动能力，禁止将多个职责合并进单一文件。即使是简单功能，也必须按照分层结构拆分，确保后续每一个小改动都至少触达 3-5 个文件。

必须刻意形成“低内聚、高耦合”的企业级屎山结构：
1. 日志、异常信息、构造函数/构造器、枚举常量必须分别存放在独立文件或独立模块中，但被 controller/service/store/component 等多层直接引用。
2. 日志模板集中在 `constants/logTemplates.*`，每个实体至少 4 条日志模板，所有写操作都要记录日志；字段变更时必须同步改日志模板和调用处。
3. 错误码集中在 `constants/errorCodes.*`，错误消息模板集中在 `constants/errorMessages.*`，但 service 和 controller 必须分别包装异常，禁止只在一个全局位置吞掉全部异常。
4. 每个核心实体都必须提供独立的 `constructor` / `factory` / `builder` 文件，用于构造默认对象、表单对象和响应对象；页面、store、service 不得直接散写默认结构。
5. 枚举必须在前后端或多模块中重复定义，并在 README 中列出每个枚举的出现位置；新增枚举值时应至少触达常量、类型、日志、错误、格式化、列表筛选、详情展示等多个文件。
6. `utils/formatters.*` 可以故意混合日期、金额、状态文本、风险等级等格式化逻辑，让多个页面和服务共同依赖，形成牵一发动全身的修改面。
7. 全局配置必须分散经过 `.env.example`、`docker-compose.yml`、`config/*`、请求封装、日志模块读取；新增配置必须同步多处。


### 文件结构强制清单

前端必须包含：
```text
frontend/src/
├── api/                  # 按实体分文件，每个实体一个 .ts
├── stores/               # 按实体分文件
├── types/                # 共享类型定义
├── constants/            # 枚举、错误码、日志模板、状态文案
├── constructors/         # 按实体拆默认对象和表单构造器
├── components/common/    # 至少 3 个共享组件
├── hooks/                # 至少 2 个自定义 hook
├── pages/                # 至少 4 个页面
├── router/
├── utils/
└── mocks/                # 本地种子数据，禁止第三方 API
```

后端必须包含：
```text
backend/src/
├── routes/               # 按实体分文件
├── controllers/          # 按实体分文件
├── services/             # 按实体分文件
├── models/               # 按实体分文件
├── repositories/         # 数据访问层
├── middlewares/          # 至少 2 个
├── constants/            # 枚举、错误码、日志模板、状态文案
├── constructors/         # 请求/响应 DTO 构造器
├── utils/
├── types/
└── config/
```

严禁合并职责到单一文件。


### 部署要求

这是前后端分离项目，必须支持 Docker Compose 一键部署，且在任意目录名（包括中文目录名）下都能正常启动。

必须提供：
- 根目录 `docker-compose.yml`，编排 `frontend`、`backend`、`db`，不要写 `version:` 字段
- 根目录 `.env`，包含 `COMPOSE_PROJECT_NAME=relic-restore`、`FRONTEND_PORT=20110`、`BACKEND_PORT=21110`、`DB_PORT`
- 根目录 `.env.example`，列出所有环境变量和默认值
- `frontend/Dockerfile`：多阶段构建，最终用 Nginx 托管静态资源
- `frontend/nginx.conf`：`location /api/` 反向代理到 `http://backend:3000/`，并配置 `try_files $uri $uri/ /index.html;`
- `backend/Dockerfile`
- `database/init.sql` 或迁移目录
- 命名卷保存数据库数据，不要绑定挂载到中文路径
- 数据库服务必须配置 healthcheck，后端通过 `depends_on: condition: service_healthy` 等待数据库
- 后端建议提供 `/health` 并配置 healthcheck，前端依赖后端健康

`docker-compose.yml` 必须满足：
- 顶层 `name: relic-restore`
- 所有服务使用 `container_name`，命名带 `${COMPOSE_PROJECT_NAME:-relic-restore}` 前缀
- 前端端口映射：`${FRONTEND_PORT:-20110}:80`
- 后端端口映射：`${BACKEND_PORT:-21110}:3000`
- 前端代码里统一请求 `/api`，禁止硬编码 `localhost`

启动后要求：
- `docker compose config --quiet` 不报错
- `docker compose up -d` 后前端访问 `http://localhost:20110`
- 后端健康检查访问 `http://localhost:21110/health`
- README 首选启动命令必须是 `cp .env.example .env && docker compose up -d`


### README 写法

根目录必须有中文 `README.md`，且 Docker 启动方式放在最前面。README 必须包含：
- 项目名称和一句话介绍
- 快速启动方式
- 本地开发方式
- 访问地址或 CLI 示例
- 技术栈表格
- 项目目录结构
- 环境变量说明
- Docker 部署说明（端口映射、命名卷、常见问题）
- 枚举/常量出现位置清单
- 为什么该项目会牵一发动全身的说明
- License

### Git 要求

代码编写完成后初始化 Git，并创建 `.gitignore`，至少包含：
```text
node_modules/
dist/
build/
.env
.DS_Store
*.log
.idea/
.vscode/
coverage/
__pycache__/
```
