export interface RelicItem {
  id: number;
  relic_code: string;
  name: string;
  era: string;
  material: string;
  collection_level: string;
  storage_location: string;
  current_condition: string;
}
